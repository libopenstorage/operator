sudo: required
dist: trusty
language: go
go:
  - 1.11.2
script:
  - make && make test && make olm-verify
notifications:
  email:
    on_success: always
    on_failure: always
after_success:
  - bash <(curl -s https://codecov.io/bash)
