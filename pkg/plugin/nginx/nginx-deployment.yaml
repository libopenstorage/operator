kind: Deployment
apiVersion: apps/v1
metadata:
  name: portworx-console-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      name: portworx-console-proxy
  template:
    metadata:
      labels:
        name: portworx-console-proxy
    spec:
      volumes:
        - name: nginx-conf
          configMap:
            name: nginx-conf
        - name: nginx-certs
          secret:
            secretName: portworx-console-proxy
      containers:
        - name: nginx
          image: nginxinc/nginx-unprivileged:1.23
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: https
              containerPort: 8443
              protocol: TCP
          volumeMounts:
            - name: nginx-conf
              mountPath: /etc/nginx
            - name: nginx-certs
              mountPath: /etc/nginx/certs